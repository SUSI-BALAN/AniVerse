import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";
const sql=readFileSync(new URL("../migrations/postgres/001_user_data.sql",import.meta.url),"utf8");
describe("PostgreSQL migration",()=>{it("adds ownership and compound uniqueness to every private table",()=>{for(const table of["favorites","watchlist","app_settings","search_history","recently_viewed","episode_progress","watch_history"]){expect(sql).toContain(`public.${table}`);expect(sql).toContain(`alter table public.${table} enable row level security`);}expect(sql).toContain("unique(user_id, anilist_id, episode_number)");expect(sql).toContain("unique(user_id, anilist_id)");});it("uses authenticated auth.uid policies and no open user-data policy",()=>{expect(sql).toContain("to authenticated");expect(sql).toContain("(select auth.uid()) = user_id");expect(sql).not.toMatch(/using\s*\(\s*true\s*\)/i);expect(sql).not.toMatch(/to\s+anon/i);});});
